#include <stdio.h>
int main(){
    int x;
    int y;
    x = 0;
    while (x <= 9){
        printf("x = %d\n",x);
        x++;
    }
   y = 0;
   while ( y <= 9) {
    y++;
    printf("y = %d\n",y);
   }

   return 0;
}
